import styled from 'styled-components/native'
import { Image } from '../../components'

export const MimirLogo = styled(Image)`
	align-self: center;
	width: 100%;
	height: 120px;
	resize-mode: contain;
`
